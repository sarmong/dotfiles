#!/bin/sh

Darwin="bash git kitty nvim ranger scripts skhd spacebar vim yabai alacritty newsboat"

Linux="bash git bspwm awesome i3 kitty nvim polybar ranger scripts sxhkd vim alacritty newsboat"

if [ "$(uname)" = "Darwin" ]; then
    for dir in ${Darwin}; do
        stow -v --ignore=".DS_Store" "$(basename "$dir")"
    done
elif [ "$(uname)" = "Linux" ]; then
    for dir in ${Linux}; do
        stow -v --ignore=".DS_Store" "$(basename "$dir")"
    done
fi

