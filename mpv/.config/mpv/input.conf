# See https://raw.githubusercontent.com/mpv-player/mpv/master/etc/input.conf

q ignore

# Open current file with default application
# Useful to open YouTube videos in a browser
o run "/bin/sh" "-c" "xdg-open $(mpvc --format '%path%')"
