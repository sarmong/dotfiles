#!/bin/sh

sessions=$(ls ~/docs/work)

selected=$(echo "$sessions" | rofi -dmenu | awk -F ':' '{print $1}')

[ -z "$selected" ] && exit 1

"$TERMINAL" --directory ~/docs/work/"$selected" -e workplace "$selected"
