#!/bin/sh

colors="$XDG_CONFIG_HOME/alacritty/colors.yml"
schemes="$XDG_CONFIG_HOME/alacritty/colorschemes"

options="\
gruvbox-dark
gruvbox-light
tomorrow-night
"

choice=$(echo "$options" | rofi -dmenu -i -p 'Select theme:')

cp "$schemes/$choice.yml" "$colors"
