#!/usr/bin/env sh

list_of_files=$(fd --type f -E '.*' -E 'node_modules' . "$HOME" | cut -d/ -f4-)

echo "$list_of_files" | rofi -dmenu -i -p "Select file" -multi-select | xargs -d '\n' dragon-drop -a
