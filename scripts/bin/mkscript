#!/bin/sh

if [ -z "$1" ]; then
  echo "No arguments provided"
  exit 1
fi

touch ~/dotfiles/scripts/bin/"$1"
chmod +x ~/dotfiles/scripts/bin/"$1"
ln -s ~/dotfiles//scripts/bin/"$1" ~/bin/"$1"
echo "#!/bin/sh" > ~/bin/"$1"
nvim ~/bin/"$1"
